<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
  </head>

  <body>
    <%- include('./partials/header') %>
    <main class="form-signin">
      <div>
        <a class="logo" href="/">CLESSIDRA</a>
      </div>
      <form
        method="post"
        action="/users/registro"
        enctype="multipart/form-data"
      >
      
        <h1 class="form-title h3 mb-3 fw-normal">
          Bienvenido/a!<br />Por favor registrate con tus datos:
        </h1>
       
        <div class="form-floating">
          <input
            type="text"
            class="form-control"
            id="nombre"
            name="userName"
            placeholder="Juan"
          />
          <label for="nombre">Nombre</label>
          <% if(locals.errors && errors.userName){ %> 
         <p class="errorText"> <%= errors.userName.msg  %> </p>
         <% } %> 
        </div>
        <div class="form-floating">
          <input
            type="text"
            class="form-control"
            id="apellido"
            name="userLastName"
            placeholder="Perez"
          />
          <label for="apellido">Apellido</label>
          <% if(locals.errors && errors.userLastName){ %> 
            <p class="errorText"> <%= errors.userLastName.msg  %> </p>
            <% } %> 
        </div>
        <div class="form-floating">
          <input
            type="email"
            class="form-control"
            id="email"
            name="userEmail"
            placeholder="name@example.com"
          />
          <label for="email">Correo electrónico</label>
          <% if(locals.errors && errors.userEmail){ %> 
            <p class="errorText"> <%= errors.userEmail.msg  %> </p>
            <% } %> 
        </div>
        <div class="form-floating">
          <input
            type="email"
            class="form-control"
            id="emailconfirm"
            name="userEmailConfirm"
            placeholder="name@example.com"
          />
          <label for="emailconfirm">Confirmar correo electrónico</label>
        </div>
        <div class="form-floating">
          <input
            type="password"
            class="form-control"
            id="password"
            name="userPassword"
            placeholder="Contraseña"
          />
          <label for="password">Elegir contraseña</label>
          <% if(locals.errors && errors.userPassword){ %> 
            <p class="errorText"> <%= errors.userPassword.msg  %> </p>
            <% } %> 
        </div>
        <div class="form-floating">
          <input
            type="password"
            class="form-control pass-form"
            id="passwordconfirm"
            name="userPasswordConfirm"
            placeholder="Confirmar contraseña"
          />
          <label for="passwordconfirm">Confirmar contraseña</label>
        </div>
        <br />
        <div class="checkbox mb-3">
          <label>
            <input type="checkbox" value="acceptterms" name="userTermsAccept" />
            Acepto los <a href="#!">Términos y Condiciones</a>
            <% if(locals.errors && errors.userTermsAccept){ %> 
              <p class="errorText"> <%= errors.userTermsAccept.msg  %> </p>
              <% } %> 
          </label>
        </div>
        <button class="btn-enviar w-100" type="submit">Registrarme</button>
        <% } %> 
      </form>
    </main>

    <%- include('./partials/footer') %>
  </body>
</html>
